/*
Use Linked List to implement this.
Each node stores value and points to next element.
When try to insert after max_size, Update head and tail pointers as follows
head = head.next;
tail.next = newNode
tail = newNode
Insertion takes o(1)
*/
